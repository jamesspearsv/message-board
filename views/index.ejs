<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="./styles.css" />
  </head>
  <body>
    <%- include('nav') %>
    <main>
      <p>Hello, World! Template</p>
      <% messages.map((message) => { %>
      <div class="message">
        <p class="name"><%= message.user %></p>
        <p class="added"><%= new Date(message.added).toLocaleDateString() %></p>
        <p class="text"><%= message.text %></p>
      </div>
      <% }) %>
    </main>
  </body>
</html>
